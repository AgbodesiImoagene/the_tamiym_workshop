import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminShippingZonesPage() {
  return (
    <AdminPlaceholderPage
      activeNav="shipping"
      eyebrow="Shipping"
      title="Shipping zones placeholder"
      description="Zone and rate tooling will be added later, but the admin shell already reserves its navigation and route structure."
    />
  );
}
