import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminShippingZoneDetailPage() {
  return (
    <AdminPlaceholderPage
      activeNav="shipping"
      eyebrow="Shipping detail"
      title="Zone detail placeholder"
      description="This route exists so future shipping deep links already have a reserved place in the admin IA."
    />
  );
}
