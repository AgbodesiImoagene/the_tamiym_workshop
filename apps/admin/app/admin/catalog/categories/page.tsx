import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminCatalogCategoriesPage() {
  return (
    <AdminPlaceholderPage
      activeNav="catalog"
      eyebrow="Catalog"
      title="Categories placeholder"
      description="Category planning remains deferred, but the route is present so the admin shell already reflects the intended catalog information architecture."
    />
  );
}
