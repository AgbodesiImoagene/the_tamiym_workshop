import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminSiteSettingsPage() {
  return (
    <AdminPlaceholderPage
      activeNav="settings"
      eyebrow="Settings"
      title="Site settings placeholder"
      description="Site-level pricing and payout policy controls are visible in the IA now, with full settings implementation reserved for a later phase."
    />
  );
}
