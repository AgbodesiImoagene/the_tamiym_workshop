import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminModerationDesignDetailPage() {
  return (
    <AdminPlaceholderPage
      activeNav="moderation"
      eyebrow="Moderation detail"
      title="Design detail placeholder"
      description="The detail route is reserved now so moderation tasks can later attach to a stable information architecture."
    />
  );
}
