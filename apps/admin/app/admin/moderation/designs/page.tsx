import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminModerationDesignsPage() {
  return (
    <AdminPlaceholderPage
      activeNav="moderation"
      eyebrow="Moderation"
      title="Design moderation placeholder"
      description="Design moderation stays visible in the navigation model now so future moderation work can plug into the same operations shell."
    />
  );
}
