import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminBulkPricingPage() {
  return (
    <AdminPlaceholderPage
      activeNav="pricing"
      eyebrow="Pricing"
      title="Bulk pricing placeholder"
      description="Bulk-pricing controls are accounted for in the admin information architecture without expanding the first implementation slice beyond operations-critical flows."
    />
  );
}
