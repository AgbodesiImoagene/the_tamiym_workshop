import { AdminPlaceholderPage } from '@/components/admin-placeholder-page';

export default function AdminPricingDiscountsPage() {
  return (
    <AdminPlaceholderPage
      activeNav="pricing"
      eyebrow="Pricing"
      title="Discounts placeholder"
      description="Discount management is represented in navigation now, but implementation remains intentionally scoped for a later admin phase."
    />
  );
}
