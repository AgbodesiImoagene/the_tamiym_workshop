# Tamiym Workshop Admin

Admin frontend built with Next.js.

## Current state

This app is still early-stage. The root page is the default starter page, with a few route files present for future admin and auth flows.

## Run locally

From the repo root:

```bash
pnpm --filter admin dev
```

Default local URL:

- `http://localhost:3003`
