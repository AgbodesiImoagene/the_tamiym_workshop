# Tamiym Workshop App

Customer-facing app built with Next.js.

## Current state

This app is still early-stage. The root page is the default starter page, with a few route files added for future auth and dashboard work.

## Run locally

From the repo root:

```bash
pnpm --filter app dev
```

Default local URL:

- `http://localhost:3002`
