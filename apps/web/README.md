# Tamiym Workshop Web

Public marketing-site app built with Next.js.

## Current state

This app is still mostly scaffold-level. The root page is the default starter page and should not be treated as production-ready marketing content.

## Run locally

From the repo root:

```bash
pnpm --filter web dev
```

Default local URL:

- `http://localhost:3000`
