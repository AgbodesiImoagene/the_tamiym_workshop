# Spaces / S3-compatible backend — copy to backend.hcl (gitignored).
# Never commit real keys.
bucket     = "ttw-tofu-state"
endpoint   = "https://lon1.digitaloceanspaces.com"
access_key = "CHANGE_ME"
secret_key = "CHANGE_ME"
