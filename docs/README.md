# AI Implementation Guide — Tamiym Workshop

This guide translates the Project Requirements Document (PRD) into technical, implementation-ready instructions for an AI coding agent and human developers.

**Source of truth:** the PRD and the client Figma designs. The implementation must maintain functional and visual parity with the approved designs and the PRD scope.  
The PRD defines: public website + customer dashboard + design workshop + checkout/orders + fundraising + admin dashboard + basic analytics, with a hard delivery target. (See PRD.)

## Non-negotiables (must follow)

1. **PRD parity:** implement in-scope features only; do not invent new flows or expand scope.
2. **Design parity:** do not redesign UI. Only small feasibility tweaks allowed (spacing, responsive adjustments) without changing intent.
3. **Nigeria-only initial release:** shipping + payments are Nigeria-only initially.
4. **Paystack-only for now:** implement Paystack as the only payment provider, but via an interface so we can add providers later.
5. **Admin required OOTB:** admin dashboard must support order/product/inventory management + moderation + basic analytics as per PRD.
6. **Built-in observability:** structured logs + metrics + traces (OpenTelemetry preferred).
7. **Monorepo:** single repo with shared packages.
8. **Testing required:** comprehensive unit + integration tests with high coverage; no critical module ships without tests.
9. **Documentation is mandatory and non-negotiable.**
   - API documentation **must** be autogenerated via Swagger/OpenAPI and kept accurate.
   - All public APIs, DTOs, and domain workflows must be documented.
   - Code must include clear inline documentation for:
     - non-trivial business logic
     - state transitions
     - payment, payout, and webhook handling
     - admin-only or security-sensitive logic
   - Documentation must be updated **as part of the same change** whenever behavior or contracts change.
   - Outdated or misleading documentation is considered a defect.
10. **Swagger/OpenAPI is a first-class deliverable, not an afterthought.**
    - Swagger must reflect the real behavior of the system.
    - Endpoints without Swagger annotations are considered incomplete.
    - Changes to request/response shapes must update OpenAPI definitions immediately.

## What to read first

1. `01-architecture.md` — system architecture & key design decisions
2. `02-repo-structure.md` — monorepo layout & conventions
3. `03-backend.md` — NestJS modules, APIs, workflows
4. `04-frontend.md` — Next.js apps, components, UI conventions
5. `05-design-workshop.md` — Fabric.js editor model + storage
6. `06-payments-and-payouts-paystack.md` — payment & payout flows
7. `07-notifications.md` — domain events & owner alerts
8. `08-analytics-and-reporting.md` — OOTB analytics scope
9. `09-observability-otel.md` — OTel setup, logging, metrics, tracing
10. `10-deployment-and-environments.md` — environments, secrets, deployment
11. `11-development-setup.md` — local dev environment setup & workflows
12. `12-testing-strategy.md` — unit/integration tests + coverage requirements

## Output expectations for the AI agent

- Make incremental PRs with small changes.
- Add tests for critical logic (payments webhooks, order state transitions).
- Keep naming consistent with this guide.
- Never store secrets in code; use environment variables.
- Provide a short implementation note in PR descriptions.

## PRD scope reminder (summary)

- Public marketing pages
- Customer dashboard (products, designs, orders, fundraising, settings)
- Design Workshop (upload artwork, add text, front/back/sleeve preview, save/share/duplicate)
- Checkout & orders (shipping info, payments, order history/status)
- Fundraising module (create campaign, configure, share link, performance snapshot)
- Admin dashboard (orders, products, inventory, fundraising admin, moderation, basic analytics)
- Basic analytics/reporting + CSV exports

See `99-prd-traceability.md` for the PRD → implementation mapping.

## Mandatory Rule: Prefer Tools and CLIs Over Manual Generation

The AI agent must **always prefer using existing tools, CLIs, and generators**
instead of manually producing output that can be reliably created by tools.

This rule is **strict and non-negotiable**.

### Required behavior

- If a task can be accomplished via a well-known CLI, generator, or automation tool,
  the tool **must be used**.
- Manual generation is only acceptable when:
  1. no suitable tool exists, or
  2. the task is inherently creative or domain-specific.

### Explicit examples (must follow)

- Use official CLIs for project scaffolding:
  - `nest new` for NestJS apps
  - `create-next-app` for Next.js apps
- Use Prisma CLI for all schema and database changes:
  - `prisma init`
  - `prisma migrate`
  - `prisma generate`
- Use Tailwind tooling for design tokens and theme configuration.
- Use OpenTelemetry SDKs and official instrumentation packages instead of custom tracing code.
- Use Paystack SDKs or official API clients where available.
- Use linters, formatters, and code generators rather than hand-formatting.
- Use color pickers, calculators, and validation tools instead of guessing values.
- Use Docker, docker-compose, or platform CLIs rather than ad-hoc scripts.

### Explicitly discouraged behavior

- Hand-writing boilerplate that a CLI would generate.
- Guessing configuration values instead of deriving them via tools.
- Manually crafting migrations instead of using Prisma migrations.
- Writing custom implementations where mature, well-supported libraries exist.

### Enforcement principle

Using tools improves:

- correctness
- consistency
- maintainability
- velocity
- reproducibility

Failure to use appropriate tools is considered a **quality defect**, even if the
resulting code “works”.

## Milestone Checklist (delivery-oriented)

This checklist is the recommended build order to hit the end-of-February delivery target.  
Each milestone should result in a shippable increment and must include basic tests + telemetry.

### M0 — Repo & Foundations (Day 1–2) ✅ COMPLETE

- [x] Monorepo scaffold (pnpm + turborepo)
- [x] Apps created: `web`, `app`, `admin`, `api`
- [x] Shared packages: `ui`, `types`, `config`
- [x] Tailwind tokens aligned to Figma (colors, spacing, typography)
- [x] Linting/formatting + pre-commit checks
- [x] Basic CI pipeline (typecheck + lint + unit tests)

### M1 — Backend Skeleton + OpenAPI (Day 2–3) ✅ COMPLETE

- [x] NestJS API bootstrapped with Prisma + Postgres
- [x] Global validation pipe (DTO validation)
- [x] Auth module skeleton (JWT cookies)
- [x] Role model: CUSTOMER / ORGANIZER / ADMIN
- [x] **Swagger/OpenAPI auto-generated** and accessible (e.g., `/docs`)
- [x] Health endpoint + structured logging + OTel baseline

### M2 — Auth + User Accounts (Day 3–5)

- [ ] Register/login/logout
- [ ] Profile endpoints + shipping address management
- [ ] Admin login (role-protected)
- [ ] Frontend auth flows wired for `app` and `admin`

### M3 — Products & Catalog (Week 1)

- [ ] Product categories + product listing + product detail
- [ ] Variants (size/color) + availability flags
- [ ] Admin product CRUD (basic)
- [ ] Basic search/filter support (category, availability)

### M4 — Design Workshop v1 (Week 1–2)

- [ ] Fabric.js editor scaffold
- [ ] Printable bounds per product view (front/back/sleeve)
- [ ] Text layers + image upload layers
- [ ] Save design (structured model) + thumbnail
- [ ] Duplicate design
- [ ] Share link (read-only)

### M5 — Checkout + Paystack Payments (Week 2)

- [ ] Cart/checkout flow (shipping info)
- [ ] Create order in `PENDING_PAYMENT`
- [ ] Paystack initiate transaction
- [ ] Paystack webhook verification + idempotent processing
- [ ] Transition to `PAID` + confirmation view
- [ ] Owner notification: OrderPlaced / PaymentConfirmed

### M6 — Orders (Customer + Admin) (Week 2–3)

- [ ] Customer order history + order detail + status display
- [ ] Admin order list + order detail + status updates
- [ ] Refund initiation flow (policy-driven) + notifications

### M7 — Fundraising (Week 3)

- [ ] Campaign creation wizard (select/design → story → settings)
- [ ] Public fundraiser page (shareable)
- [ ] Campaign performance snapshot (basic)
- [ ] Admin campaign review/disable

### M8 — Moderation + Inventory (Week 3–4)

- [ ] Moderation queue for designs/assets (flag/approve/reject)
- [ ] Inventory management basics (variant availability, out-of-stock)
- [ ] Admin audit notes (optional but helpful)

### M9 — Analytics & Reporting (Week 4)

- [ ] Admin ops dashboard: orders/revenue/campaign metrics
- [ ] Filters (date range, product, campaign)
- [ ] CSV export for key tables

### M10 — Hardening + Release (Final week)

- [ ] Error handling polish + empty states
- [ ] Performance pass on core flows
- [ ] Security pass (admin separation, webhook verification)
- [ ] Observability completeness (metrics + traces on critical paths)
- [ ] UAT checklist + acceptance criteria verification (PRD)

## AI Execution Philosophy

The AI agent is expected to behave like a disciplined senior engineer, not a
creative prototype generator.

Priorities, in order:

1. Correctness
2. Maintainability
3. Observability
4. Testability
5. Documentation
6. Speed

Shortcuts that compromise long-term quality are not acceptable, even if they
appear to save time in the short term.

### Documentation + Tooling Rule

Any feature that introduces:

- new API endpoints
- new database schema
- new background jobs
- new external integrations

must include:

- updated Swagger/OpenAPI docs
- updated inline code documentation
- updated tests
- use of official tooling/CLIs where applicable
