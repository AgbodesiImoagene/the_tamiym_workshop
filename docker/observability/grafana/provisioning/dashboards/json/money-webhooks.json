{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "links": [
    {
      "title": "Runbook: Webhooks & money",
      "url": "docs/runbooks/webhooks-money.md",
      "type": "link",
      "icon": "doc"
    }
  ],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/webhooks-money.md",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (outcome) (rate(tamiym_webhook_events_total[5m]))",
          "legendFormat": "{{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Webhook events by outcome",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/webhooks-money.md — alert TamiymChargeSettlementRejected",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (outcome) (rate(tamiym_charge_settlement_total[5m]))",
          "legendFormat": "{{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Charge settlement outcomes",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/webhooks-money.md — alert TamiymRefundSettlementFailed",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (outcome) (rate(tamiym_refund_settlement_total[5m]))",
          "legendFormat": "{{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Refund settlement outcomes",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/webhooks-money.md — alert TamiymPayoutFailure",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (outcome) (rate(tamiym_payouts_total[5m]))",
          "legendFormat": "payout {{outcome}}",
          "refId": "A"
        },
        {
          "expr": "sum by (outcome) (rate(tamiym_payout_runs_total[5m]))",
          "legendFormat": "run {{outcome}}",
          "refId": "B"
        }
      ],
      "title": "Payout and payout-run outcomes",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["observability", "money", "webhooks", "ttw-051"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "Money & Webhooks",
  "uid": "ttw-051-money-webhooks",
  "version": 1
}
