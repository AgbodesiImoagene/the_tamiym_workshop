{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "links": [
    {
      "title": "Runbook: Media security",
      "url": "docs/runbooks/media-security.md",
      "type": "link",
      "icon": "doc"
    },
    {
      "title": "Runbook: Auth abuse",
      "url": "docs/runbooks/auth-abuse.md",
      "type": "link",
      "icon": "doc"
    }
  ],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/media-security.md — alert TamiymMediaVirusScanFailure",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (outcome) (rate(tamiym_media_virus_scan_total[5m]))",
          "legendFormat": "{{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Media virus scan outcomes",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/media-security.md",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (reason) (rate(tamiym_media_fetch_denied_total[5m]))",
          "legendFormat": "{{reason}}",
          "refId": "A"
        }
      ],
      "title": "Remote media fetch denials",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/auth-abuse.md — alert TamiymAuthThrottleLimited",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (surface, outcome) (rate(tamiym_auth_throttle_total[5m]))",
          "legendFormat": "{{surface}} {{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Auth throttle decisions",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Runbook: docs/runbooks/auth-abuse.md — alert TamiymAuthLoginDeniedSpike",
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (outcome) (rate(tamiym_auth_login_total[5m]))",
          "legendFormat": "{{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Auth login outcomes",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["observability", "media", "auth", "ttw-051"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "Media & Auth",
  "uid": "ttw-051-media-auth",
  "version": 1
}
